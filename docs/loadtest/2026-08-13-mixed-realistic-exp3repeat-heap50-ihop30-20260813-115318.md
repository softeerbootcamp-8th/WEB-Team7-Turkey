측정 구간: <!-- epoch 1786589657~1786589837 --> 180초, testid=`mixed-realistic-exp3repeat-heap50-ihop30-20260813-115318`

| 항목 | 값 |
|---|---|
| **--- k6(부하 발생기) ---** | |
| 총 요청 | 218,125건 |
| 평균 처리량 | 1,212 req/s |
| 피크 처리량(15초 해상도) | 1,494 req/s |
| 실패율(구간 최댓값) | 0.00% |
| p95(구간 최댓값·참고) | 2,569.15 ms |
| p99(구간 최댓값·참고) | 2,569.15 ms |
| 최대 VU | 1,700 |
| 드롭된 iteration | 0건 |
| **--- 앱(Micrometer) ---** | |
| 서버측 평균 처리시간 | 19.81 ms |
| 5xx 응답 | 0건 |
| Hikari 활성 최대 | 10 |
| Hikari 대기 최대 | 284 |
| JVM 플랫폼 스레드 최대 | 38 |
| Tomcat 현재 연결 최대 | 1,706 |
| GC 횟수 | 177회 |
| GC 정지 합계 | 1.37초 |
| GC 정지 비율 | 0.8% |
| 힙 사용 최대 | 451 MiB |
| 힙 상한 | 512 MiB |
| MySQL 실행 스레드 최대 | 4 |
| k6 비정상 응답 | 0건 |
| **--- 요청당 왕복 수 ---** | |
| MySQL 문장/요청 | 13.90 |
| Redis 커맨드/요청 | 5.08 |

- 런 단위 p95/p99 는 k6 종료 시 터미널 summary 를 쓴다(위 값은 구간 최댓값).
- 요청당 왕복 수의 분자에는 exporter 스크레이프·백그라운드 스케줄러(배차 만료 스캐너 1분 주기)가 함께 들어가 **약간 높게** 나온다. 창이 setup 을 제외하도록 고친 뒤에는 이 오차가 작다(실측: 이론값 13.03/5.00 대비 12.9~13.1/4.95~5.02). 부하가 낮거나 창이 짧으면 상대 오차가 커지므로 그때만 바닥값(부하 없는 같은 길이 구간)을 빼서 본다.

### 단계별 지표 — 1단 (각 단의 유지 구간만, 램프 구간 제외)

| 항목 | 1700 VU |
|---|---|
| 총 요청 | 184,710건 |
| 평균 처리량 | 1,478 req/s |
| 피크 처리량(15초 해상도) | 1,494 req/s |
| 실패율(구간 최댓값) | 0.00% |
| p95(구간 최댓값·참고) | 1,568.79 ms |
| p99(구간 최댓값·참고) | 2,369.07 ms |
| 최대 VU | 1,700 |
| 드롭된 iteration | 0건 |
| 서버측 평균 처리시간 | 6.84 ms |
| 5xx 응답 | 0건 |
| Hikari 활성 최대 | 10 |
| Hikari 대기 최대 | 284 |
| JVM 플랫폼 스레드 최대 | 38 |
| Tomcat 현재 연결 최대 | 1,706 |
| GC 횟수 | 121회 |
| GC 정지 합계 | 0.84초 |
| GC 정지 비율 | 0.7% |
| 힙 사용 최대 | 451 MiB |
| 힙 상한 | 512 MiB |
| MySQL 실행 스레드 최대 | 4 |
| k6 비정상 응답 | 0건 |
| MySQL 문장/요청 | 13.41 |
| Redis 커맨드/요청 | 4.72 |
- raw 시계열(15/15 계열, 5초 간격): `docs/loadtest/2026-08-13-mixed-realistic-exp3repeat-heap50-ihop30-20260813-115318-raw.json` — 표는 구간을 한 숫자로 접으므로 처리량이 꺾인 지점·지연이 튄 구간은 이 파일로 본다.
- 같은 파일의 `panels` 에 Grafana 대시보드 패널 쿼리도 담았다: 138개 값 있음 / 실행 193 (빈결과 46, repeat 변수라 건너뜀 9). 화면에서 보는 값과 같은 쿼리다.
