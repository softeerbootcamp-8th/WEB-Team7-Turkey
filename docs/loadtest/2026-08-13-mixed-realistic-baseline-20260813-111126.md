측정 구간: <!-- epoch 1786587146~1786587316 --> 170초, testid=`mixed-realistic-baseline-20260813-111126`

| 항목 | 값 |
|---|---|
| **--- k6(부하 발생기) ---** | |
| 총 요청 | 2,839건 |
| 평균 처리량 | 17 req/s |
| 피크 처리량(15초 해상도) | 44 req/s |
| 실패율(구간 최댓값) | 100.00% |
| p95(구간 최댓값·참고) | 150,005.46 ms |
| p99(구간 최댓값·참고) | 150,005.46 ms |
| 최대 VU | 1,700 |
| 드롭된 iteration | 0건 |
| **--- 앱(Micrometer) ---** | |
| 서버측 평균 처리시간 | - |
| 5xx 응답 | 0건 |
| Hikari 활성 최대 | - |
| Hikari 대기 최대 | - |
| JVM 플랫폼 스레드 최대 | - |
| Tomcat 현재 연결 최대 | - |
| GC 횟수 | - |
| GC 정지 합계 | - |
| GC 정지 비율 | - |
| 힙 사용 최대 | 65 MiB |
| 힙 상한 | - |
| MySQL 실행 스레드 최대 | 2 |
| k6 비정상 응답 | 2,775건 |
| **--- 요청당 왕복 수 ---** | |
| MySQL 문장/요청 | 4.34 |
| Redis 커맨드/요청 | 0.04 |

- 런 단위 p95/p99 는 k6 종료 시 터미널 summary 를 쓴다(위 값은 구간 최댓값).
- 요청당 왕복 수의 분자에는 exporter 스크레이프·백그라운드 스케줄러(배차 만료 스캐너 1분 주기)가 함께 들어가 **약간 높게** 나온다. 창이 setup 을 제외하도록 고친 뒤에는 이 오차가 작다(실측: 이론값 13.03/5.00 대비 12.9~13.1/4.95~5.02). 부하가 낮거나 창이 짧으면 상대 오차가 커지므로 그때만 바닥값(부하 없는 같은 길이 구간)을 빼서 본다.
- **비어 있는 항목**: 서버측 평균 처리시간, Hikari 활성 최대, Hikari 대기 최대, JVM 플랫폼 스레드 최대, Tomcat 현재 연결 최대, GC 횟수, GC 정지 합계, GC 정지 비율, 힙 상한 — 해당 exporter 가 부하 대상을 보고 있는지 확인(docker-compose.loadtest.yml).

### 단계별 지표 — 1단 (각 단의 유지 구간만, 램프 구간 제외)

| 항목 | 1700 VU |
|---|---|
| 총 요청 | 1,667건 |
| 평균 처리량 | 13 req/s |
| 피크 처리량(15초 해상도) | 36 req/s |
| 실패율(구간 최댓값) | 100.00% |
| p95(구간 최댓값·참고) | 60,004.49 ms |
| p99(구간 최댓값·참고) | 60,004.49 ms |
| 최대 VU | 1,700 |
| 드롭된 iteration | 0건 |
| 5xx 응답 | 0건 |
| MySQL 실행 스레드 최대 | 2 |
| k6 비정상 응답 | 1,667건 |
| MySQL 문장/요청 | 0.06 |
| Redis 커맨드/요청 | 0.05 |
- raw 시계열(15/15 계열, 5초 간격): `docs/loadtest/2026-08-13-mixed-realistic-baseline-20260813-111126-raw.json` — 표는 구간을 한 숫자로 접으므로 처리량이 꺾인 지점·지연이 튄 구간은 이 파일로 본다.
- 같은 파일의 `panels` 에 Grafana 대시보드 패널 쿼리도 담았다: 142개 값 있음 / 실행 193 (빈결과 42, repeat 변수라 건너뜀 9). 화면에서 보는 값과 같은 쿼리다.
