# 실제 값은 .env.local(gitignore됨, *.local)에 각자 채운다.

# API 서버 origin. 비워두면(같은 오리진 상대 경로) 개발은 vite proxy, 배포는 CloudFront가 처리한다.
VITE_API_BASE_URL=

# 카카오맵 JavaScript SDK 키. developers.kakao.com에서 앱 생성 → Maps 활성화 → JavaScript 키 발급,
# 플랫폼(Web)에 로컬 도메인(localhost) 등록 필요.
VITE_KAKAO_MAP_KEY=
